extends ../base
include ../mixin_main/review

block content
    header
        include ../partial/logo
        a(href="/")
            h1 UOS TABLE
        include ../partial/authentication
    
    main.review_list
        h3 리뷰 조회
        each review in reviews
            +review(review)
        else
            li 작성한 리뷰가 없습니다.