extends ../base

block content 
    header
        include ../partial/logo
        a(href="/manager")
            h1 UOS TABLE
        .managerbox_authenication
            a(href="/manager/edit") 가게 수정
            a(href="/logout") 로그아웃

    main.addMenus
        h3 메뉴 추가
        .addMenus_form
            form(method="POST")
                input(type="text", required, name="menuName", maxlength="30", placeholder="메뉴명")
                input(type="text", required, name="info", maxlength="50", placeholder="상세 정보")
                input(type="number", required, name="price", placeholder="가격")
                input(type="submit", value="메뉴 추가")